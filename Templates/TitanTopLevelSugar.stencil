{% for method in type.Titan.methods %}public func {{ method.shortName }}({% for param in method.parameters %}{{ param.argumentLabel }} {{ param.name }}: {{ param.typeName }}{% if not forloop.last %}, {% endif %}{% endfor %}) {
  GlobalDefaultTitanInstance.{{ method.shortName }}({% for param in method.parameters %}{% if param.argumentLabel != "_" %}{{ param.argumentLabel }}: {% endif %}{{ param.name }}{% if not forloop.last %}, {% endif %}{% endfor %})
}

{% endfor %}
