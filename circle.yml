# CircleCI & Docker
#
# learn more at https://karthikv.net/articles/circleci-docker-flow/
# and https://circleci.com/docs/docker/

version: 2
machine:
  services:
    - docker

dependencies:
  override:
    - docker --version
    - docker build -t app .

test:
  override:
    - docker run app
